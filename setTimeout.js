//callback --> what happens (function)
//delay --> when it happens (integer, delay in milliseconds)

// setTimeout(callback, delay)

setTimeout(() => {
    console.log("second")
}, 1000)

console.log("it starts now")

console.log("it ends now")

// setTimeout(() => {
//     console.log("first")
// }, 1000)
